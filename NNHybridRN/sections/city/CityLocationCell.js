import React, { Component } from 'react';
import { SectionList, StyleSheet,  } from 'react-native';
import AppUtil from '../../utils/AppUtil';

export default class CityLocationCell extends Component {

    render() {

    }
}

const styles = StyleSheet.create({
});